import { MarkdownBoldPipe } from './markdown-bold.pipe';

describe('MarkdownBoldPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkdownBoldPipe();
    expect(pipe).toBeTruthy();
  });
});
