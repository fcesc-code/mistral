import { NewlineBreakerPipe } from './newline-breaker.pipe';

describe('NewlineBreakerPipe', () => {
  it('create an instance', () => {
    const pipe = new NewlineBreakerPipe();
    expect(pipe).toBeTruthy();
  });
});
